
<h1 class="title">Products</h1></br>

<div id="card">
    <div class="row" style="padding-left:150px  ">
        <div class="col s8 ">
            <div class="input-field">
                <input id="pName" name="pName" type="text" class="validate" required>
                <label for="pName">Product name: </label>
                <span class="helper-text" data-error="Enter the valid name"></span>
            </div>
        </div>
        <input type="hidden" name="_csrf" value="{{@root.csrf}}">
        <div class="col s4 ">
            <button class=" btn js-search" data-csrf="{{@root.csrf}}">Search </button>
        </div>
    </div>
</div>

<div id="card">
<div class="cards shown" id="products">
    {{#if products.length}}
        {{#each products}}

                <div class="col s3 ">
                    <div class="card shown">
                        <div class="card-image ">
                            <img src="{{img}}" alt="{{name}}">
                        </div>
                        <div class="card-content">
                            <span class="card-title">{{name}}</span>
                            <p>{{description}}</p>
                            <p class="price">{{price}}</p>
                        </div>
                        <div class="card-action actions" >
                            {{#if @root.isAdmin}}
                                <a class="link" href="/products/{{id}}/edit?allow=true" >Edit </a>
                            {{/if}}
                            {{#if @root.isAuth}}
                                <form action="/card/add" method="post" class="f">
                                    <input type="hidden" name="_csrf" value="{{@root.csrf}}">
                                    <input type="hidden" name="id" value="{{id}}">
                                    <button type="submit" class="btn ">Buy</button>
                                </form>
                            {{/if}}
                        </div>
                    </div>
                </div>

        {{/each}}
    {{else}}
        <h4>No products</h4>
    {{/if}}
</div>
</div>